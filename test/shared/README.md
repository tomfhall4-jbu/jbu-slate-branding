Purpose of files:

build.css
    Canonical CSS used by Slate hosted pages.

build-overrides.css
    Overrides to upstream Slate/institutional styles.

_experiments.css
    Temporary experimental CSS.
    Rules here should be promoted or removed once validated.